#!/bin/sh
#|-*- mode:lisp -*-|#
#| Sample server kick script
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp (ql:quickload '() :silent t)
  (ql:quickload :kappa-sample)
  )

(defpackage :ros.script.sample
  (:use :cl))
(in-package :ros.script.sample)

(defun main (&rest argv)
  (declare (ignorable argv))
  (kappa-sample:run))
;;; vim: set ft=lisp lisp:
